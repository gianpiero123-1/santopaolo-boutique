---
interface Props {
  activePage: 'home' | 'calendar' | 'bookings' | 'requests' | 'tasks' | 'extra' | 'stats' | 'tax';
}

const { activePage } = Astro.props;

const links: { key: Props['activePage']; label: string; href: string }[] = [
  { key: 'home', label: 'Home', href: '/admin' },
  { key: 'calendar', label: 'Calendario', href: '/admin/calendar' },
  { key: 'bookings', label: 'Prenotazioni', href: '/admin/bookings' },
  { key: 'requests', label: 'Richieste', href: '/admin/requests' },
  { key: 'tasks', label: 'Task', href: '/admin/tasks' },
  { key: 'extra', label: 'Servizi Extra', href: '/admin/extra-services' },
  { key: 'stats', label: 'Statistiche', href: '/admin/stats' },
  { key: 'tax', label: 'Tassa', href: '/admin/tax' },
];
---

<nav class="nav-foot">
  {links.map((l) => (
    <a href={l.href} class={activePage === l.key ? 'active' : ''}>{l.label}</a>
  ))}
</nav>
